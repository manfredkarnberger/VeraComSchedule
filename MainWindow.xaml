<Window x:Class="VeraCom.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VeraCom"
        mc:Ignorable="d"
        Title="VeraComTool" Height="800" Width="1000" Background="#3F3F3F">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Datei">
                <MenuItem Header="Öffnen" Click="DateiOeffnen_Click"/>
                <Separator/>
                <MenuItem Header="Beenden" Click="Beenden_Click"/>
            </MenuItem>
        </Menu>
        
        <ToolBar Grid.Row="1">
            <Button Name="BtnStart" Content="Start" Click="Start_Click"/>
            <Button Name="BtnStop" Content="Stop" Click="Stop_Click"/>
            <!-- 🔥 DIESE TEXTBOX FEHLT BEI DIR -->
            <TextBox x:Name="TxtDatabasePath"
             Width="400"
             IsReadOnly="True"
             Margin="5,0,0,0"/>
        </ToolBar>

        <!-- Senden -->
        <DataGrid Grid.Row="2"
            ItemsSource="{Binding Messages}"
            AutoGenerateColumns="False"
            Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="CAN ID" Binding="{Binding CanID}"/>
                <DataGridTextColumn Header="Payload" Binding="{Binding PayloadString}"/>
                <DataGridTextColumn Header="Cycle (ms)" Binding="{Binding CycleTimeMs}"/>
                <DataGridTextColumn Header="Tx Count" Binding="{Binding TxFrameCounter}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Empfang -->
        <DataGrid Grid.Row="3"
            ItemsSource="{Binding ReceivedMessages}"
            AutoGenerateColumns="False"
            Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="CAN ID" Binding="{Binding CanID}"/>
                <DataGridTextColumn Header="Payload" Binding="{Binding PayloadString}"/>
                <DataGridTextColumn Header="Cycle (ms)" Binding="{Binding RxCycleTime}"/>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</Window>
